version: '2'
services:
  phabricator:
    image: 'bitnami/phabricator:latest'
    depends_on:
      - mariadb
    ports:
      - '83:80'
      - '443:443'
    volumes:
      - 'phabricator_data:/bitnami'
volumes:
  phabricator_data:
    driver: local